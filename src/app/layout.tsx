import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Ascent Real Estate - Your Dream Home Awaits",
  description: "Find your perfect property with Ascent Real Estate. Browse luxury homes, apartments, and commercial spaces in Pattaya.",
  metadataBase: new URL('https://ascent-realestate.com'),
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={`${inter.className} flex flex-col min-h-screen`}>
        {children}
      </body>
    </html>
  );
}
